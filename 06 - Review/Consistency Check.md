# Consistency Check

## Current Consistency Status

The active foundation is internally aligned around one proposed MRQ and three proposed SQs. No participant/stakeholder question exists, so no interview/survey method is currently required. No technical implementation question exists, so no experiment is currently required.

## Repository-Wide Consistency Checklist

Before submission verify:

- problem statement ↔ research objective ↔ MRQ;
- MRQ ↔ exactly three SQs;
- scope ↔ modality coverage in every SQ;
- `deepfake`, `AI-generated`, `manipulated`, `detector`, `reliability`, `real-world conditions`, `current` terminology;
- methodology wording ↔ method actually executed;
- population/sample wording ↔ screening records;
- inclusion/exclusion rules ↔ Sources Index/Evidence Matrix;
- dataset/generator names and metric definitions;
- reported values ↔ source evidence;
- results ↔ conclusions;
- findings ↔ recommendations;
- citations ↔ references;
- title/question wording across all submission artefacts;
- paper page-count interpretation ↔ final rendered document.

## Corrected Foundation Inconsistencies

1. **Method label:** `structured literature review` was too easy to read as a full systematic-review claim. Active files now use `structured literature study / desk research` unless the executed method later justifies stronger terminology.
2. **Page budget:** prior wording risked treating the 8–10 pages as mainly the Results section. Active files now preserve the source wording and mark exact interpretation `To Verify`.
3. **Rubric detail:** broad B1–B5 requirements were insufficient for one-by-one assessment tracing. Child requirement IDs now capture every separate rubric row.
4. **Argument status:** unsupported intended claims are now `Evidence Needed`, not merely `Planned`.
5. **Feedback:** future peer-review obligations are separated from actual received feedback.

## Source-Material Ambiguities

### Assessment scale / aggregation

The module book says the research paper is assessed Sufficient/Insufficient. The direct assessment form also contains coloured numeric bands and a rule concerning insufficient `sub-digits`. Exact internal aggregation remains `To Verify`.

### Page-count wording

The direct assessment form requires 8–10 pages and excludes introduction, method, conclusion, discussion and source list from that stated `body` count. Abstract treatment and total-document implication are not explicit.

### Submission date

The assessment-form cover displays Friday, 25 September 2026; the module book says Friday of week 4. Treat the exact date as `To Verify` until Teams confirms it.

### Typographical/legacy wording

The school materials contain several wording/translation inconsistencies (e.g. unassessable codes and legacy `CEH paper` references). Do not propagate obvious document noise as substantive requirements.